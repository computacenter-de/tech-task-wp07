apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudbees-ci.fullname" . }}-test-connection"
  labels:
    {{- include "cloudbees-ci.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudbees-ci.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
